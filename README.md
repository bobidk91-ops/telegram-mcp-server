<<<<<<< HEAD
# Telegram MCP Server

MCP (Model Context Protocol) сервер для управления Telegram каналом с расширенными функциями для ведения блога.

## 🚀 Возможности

### Основные функции публикации
- **Отправка текстовых сообщений** - публикация постов с поддержкой HTML/Markdown разметки
- **Отправка фотографий** - публикация изображений с подписями
- **Отправка документов** - публикация файлов (PDF, документы и т.д.)

### Интерактивные функции
- **Создание опросов** - обычные опросы и викторины с правильными ответами
- **Реакции на сообщения** - отправка эмодзи как реакций на посты
- **Закрепление сообщений** - закрепление важных постов в канале

### Аналитические функции
- **Информация о канале** - получение данных о канале
- **Подсчет подписчиков** - количество участников канала

## 🛠 Технологии

- **Node.js** - серверная платформа
- **TypeScript** - типизированный JavaScript
- **MCP SDK** - протокол для интеграции с AI моделями
- **Telegram Bot API** - взаимодействие с Telegram
- **Railway** - облачная платформа для развертывания

## 📋 Требования

- Node.js >= 18.0.0
- Telegram Bot Token
- Telegram канал для публикаций

## 🔧 Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd telegram-mcp-server
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**
```bash
cp env.example .env
```

Отредактируйте `.env` файл:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHANNEL_ID=@your_channel_username
PORT=3000
NODE_ENV=production
```

4. **Соберите проект:**
```bash
npm run build
```

## 🚀 Запуск

### Локальный запуск
```bash
npm run dev
```

### Продакшн запуск
```bash
npm start
```

## 📡 Доступные инструменты MCP

### send_message
Отправка текстового сообщения в канал.

**Параметры:**
- `text` (обязательный) - текст сообщения
- `parse_mode` (опциональный) - режим разметки: HTML, Markdown, MarkdownV2

**Пример:**
```json
{
  "name": "send_message",
  "arguments": {
    "text": "Привет! Это тестовое сообщение.",
    "parse_mode": "HTML"
  }
}
```

### send_photo
Отправка фотографии в канал.

**Параметры:**
- `photo_url` (обязательный) - URL изображения
- `caption` (опциональный) - подпись к фото
- `parse_mode` (опциональный) - режим разметки

**Пример:**
```json
{
  "name": "send_photo",
  "arguments": {
    "photo_url": "https://example.com/image.jpg",
    "caption": "Красивое фото!",
    "parse_mode": "HTML"
  }
}
```

### send_document
Отправка документа в канал.

**Параметры:**
- `document_url` (обязательный) - URL документа
- `caption` (опциональный) - подпись к документу
- `parse_mode` (опциональный) - режим разметки

### send_poll
Создание опроса в канале.

**Параметры:**
- `question` (обязательный) - вопрос опроса
- `options` (обязательный) - массив вариантов ответов (2-10)
- `is_anonymous` (опциональный) - анонимный опрос (по умолчанию true)
- `type` (опциональный) - тип опроса: "regular" или "quiz"
- `correct_option_id` (опциональный) - ID правильного ответа для викторины
- `explanation` (опциональный) - объяснение для викторины

**Пример:**
```json
{
  "name": "send_poll",
  "arguments": {
    "question": "Какой ваш любимый язык программирования?",
    "options": ["JavaScript", "Python", "TypeScript", "Go"],
    "is_anonymous": true,
    "type": "regular"
  }
}
```

### send_reaction
Отправка реакции на сообщение.

**Параметры:**
- `message_id` (обязательный) - ID сообщения
- `emoji` (опциональный) - эмодзи для реакции (по умолчанию 👍)

### pin_message
Закрепление сообщения в канале.

**Параметры:**
- `message_id` (обязательный) - ID сообщения
- `disable_notification` (опциональный) - отключить уведомления

### unpin_message
Открепление сообщения.

**Параметры:**
- `message_id` (обязательный) - ID сообщения

### get_channel_info
Получение информации о канале.

**Параметры:** отсутствуют

### get_chat_member_count
Получение количества подписчиков канала.

**Параметры:** отсутствуют

## 🚂 Развертывание на Railway

1. **Подключите GitHub репозиторий к Railway:**
   - Зайдите на [railway.app](https://railway.app)
   - Создайте новый проект из GitHub репозитория

2. **Настройте переменные окружения в Railway:**
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `TELEGRAM_CHANNEL_ID` - ID или username канала
   - `PORT` - порт (Railway автоматически установит)
   - `NODE_ENV` - production

3. **Railway автоматически:**
   - Установит зависимости (`npm install`)
   - Соберет проект (`npm run build`)
   - Запустит сервер (`npm start`)

## 🔐 Безопасность

- Никогда не коммитьте файл `.env` в репозиторий
- Используйте переменные окружения для чувствительных данных
- Ограничьте доступ к боту только необходимым каналам

## 📝 Создание Telegram бота

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Получите токен бота
5. Добавьте бота в ваш канал как администратора

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🆘 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте [Issues](https://github.com/your-repo/issues) на GitHub
2. Создайте новый Issue с подробным описанием проблемы
3. Убедитесь, что все переменные окружения настроены правильно

## 🔄 Обновления

Следите за обновлениями проекта:
- Новые функции будут добавляться в соответствии с развитием MCP протокола
- Улучшения производительности и стабильности
- Расширение возможностей Telegram Bot API

---

**Создано с ❤️ для эффективного управления Telegram каналами**

=======
# telegram-mcp-server
mcp server for telegram
>>>>>>> fb54e17625e49b9948af4ef175b0c1769a85d4d5
